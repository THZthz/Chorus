ALWAYS read docs/DEVELOPER.md first and maintain it while you work. 
docs/DEVEVLOPER.md should only act as a reference, it may contain outdated information, so read the code to make sure you do not speculate functions of any file.
Modify docs/CHANGELOG.md if a major change is made after you work. 
When user reports bugs, record it first in docs/ISSUES.md, this file act as a todo list for your work. 
Keep your tone concise in these Markdown files.

If user's intent is ambiguous, be sure to ask them first.